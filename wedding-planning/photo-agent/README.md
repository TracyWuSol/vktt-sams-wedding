# PhotoAgent SAM Plugin

A SAM plugin: photo agent

This is a plugin for Solace Agent Mesh (SAM).

## Installation

Once the plugin is installed (e.g., from PyPI or a local wheel file):
```bash
sam plugin add <your-new-component-name> --plugin photo-agent
```