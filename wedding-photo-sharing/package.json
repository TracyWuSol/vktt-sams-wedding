{
  "name": "wedding-photo-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "frontend", "email-worker"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=email-worker\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend && npm run build --workspace=email-worker",
    "test": "npm run test --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
